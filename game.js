var running = false; //check to see if a simulation is currently running
var paused = false;
var map = [];
function setup(ratio){//set up the needed components
    running=true;
    var ba_num = ratio[1];
    var ga_num = ratio[0];
    var game_window = document.querySelector('canvas').getContext("2d");
}
function game(){//actual simulation
}